// Import das Tabelas
const Reserva = require("./tb_Reserva");
const Cliente = require("./tb_Cliente");

// Sincronização das Tabelas
// Reserva.sync().then(() => {
//     console.log("Tabela criada com sucesso!")
// })

// Cliente.sync().then(() => {
//     console.log("Tabela criada com sucesso!")
// })